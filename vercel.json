{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "ignoreCommand": "git rev-parse HEAD^ >/dev/null 2>&1 || exit 1; git diff --quiet HEAD^ HEAD -- . ':(exclude)schedules'",
  "functions": {
    "api/schedule/*.js": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/schedule/tick",
      "schedule": "0 6 * * *"
    }
  ]
}
